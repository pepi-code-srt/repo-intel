[project]
name = "repo-intel"
version = "2.0.0"
description = "AI-Powered GitHub Repository Analysis — Task-Based Model Routing"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "google-genai",
    "langgraph",
    "pydantic",
    "python-dotenv",
    "gitpython",
    "requests",
    "httpx",
    "websockets",
    "jinja2",
]
