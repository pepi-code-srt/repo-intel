[project]
name = "repo-intel"
version = "0.1.0"
description = "AI-Powered GitHub Repository Analysis Agent"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "langgraph",
    "langchain-core",
    "langchain-google-genai",
    "mcp",
    "gitpython",
]
